{
  "name": "pulse-calendar-scriptable",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "check:bundle": "node --check \"Pulse Calendar.js\" && node --check installer.js",
    "verify": "npm run build && npm test && npm run check:bundle"
  },
  "engines": {
    "node": ">=20"
  }
}
